let one = 1;
let two = 2;

console.log(one == "1");
console.log(one == two);

// Retorna um boolean

// Diferente de

console.log(two != "1");
console.log(one != 1);

// Operadores estritamente, o tipo dos elementos entra em comparação

console.log(one !== "1");
console.log(one !== 1);